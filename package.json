{
  "name": "glpkart-backend",
  "version": "2.1.0",
  "scripts": {
    "start": "npx prisma db push --accept-data-loss && npx prisma generate && node index.js",
    "build": "echo ok",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "fastify": "^4.28.1",
    "@fastify/cors": "^9.0.1",
    "@prisma/client": "^5.14.0",
    "prisma": "^5.14.0",
    "jsonwebtoken": "^9.0.2",
    "axios": "^1.7.2"
  }
}
